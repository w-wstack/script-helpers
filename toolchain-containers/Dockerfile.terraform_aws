FROM hashicorp/terraform:1.13.1

# Add AWS CLI so null_resource local-exec provisioners (e.g. wiring a
# Cognito CustomEmailSender trigger via `aws cognito-idp update-user-pool`)
# can run inline with `terraform apply`, without a separate host-side step.
RUN apk add --no-cache aws-cli bash
